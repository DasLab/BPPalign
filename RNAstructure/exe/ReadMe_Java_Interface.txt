To run the Java GUI, one should NOT simply run the RNAstructure.jar file.

On Windows machines, start the GUI by running the RNAstructure.bat script file.

On Linux/Macintosh machines, start the GUI by running the RNAstructureScript
script file.
